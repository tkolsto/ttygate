# Chunk 4.1 Docker and systemd Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Deliver reproducible, non-root Docker packaging and a hardened,
watchdog-enabled systemd service with executable cleanup-focused smoke tests.

**Architecture:** Build frontend assets and the Rust daemon in digest-pinned
builder stages, then copy only runtime artifacts into a fixed non-root Debian
runtime. Add a bounded daemon health-check command and native systemd readiness
and watchdog notification; validate packaging statically everywhere and
dynamically in Linux/Docker smoke jobs.

**Tech Stack:** Rust/Tokio, `sd-notify`, Docker/BuildKit, POSIX shell, systemd,
GitHub Actions.

---

### Task 1: Packaging contract harness

**Files:**
- Create: `scripts/check-packaging.sh`
- Modify: `.gitignore`

**Steps:**

1. Write assertions for the not-yet-present Dockerfile, `.dockerignore`,
   package config, systemd unit/sysusers/tmpfiles files, digest pins, non-root
   identity, `/healthz`, localhost bind, runtime ownership paths, and required
   hardening directives.
2. Run `./scripts/check-packaging.sh`.
3. Confirm it fails because the first required packaging artifact is absent.
4. Add only ignored smoke-output paths needed by the harness.
5. Re-run the checker and retain the expected packaging-artifact failure.

### Task 2: Daemon health-check command

**Files:**
- Create: `crates/ttygated/src/healthcheck.rs`
- Modify: `crates/ttygated/src/lib.rs`
- Modify: `crates/ttygated/src/main.rs`

**Steps:**

1. Add focused unit/integration assertions for exact `GET /healthz`, bounded
   response parsing, timeouts, malformed/non-200 rejection, argument grammar,
   and stable non-reflecting diagnostics.
2. Run the focused tests and confirm they fail because the command/module is
   missing.
3. Implement the smallest bounded loopback HTTP checker and CLI dispatch.
4. Re-run the focused tests and adjacent HTTP/startup tests.

### Task 3: systemd readiness and watchdog

**Files:**
- Modify: `crates/ttygated/Cargo.toml`
- Modify: `Cargo.lock`
- Create: `crates/ttygated/src/service_manager.rs`
- Modify: `crates/ttygated/src/lib.rs`
- Modify: `crates/ttygated/src/startup.rs`

**Steps:**

1. Add tests using a disposable notification socket plus pure interval tests
   for `READY=1`, negotiated half-interval watchdog cadence, disabled behavior,
   bounded task cancellation, and non-fatal absence outside systemd.
2. Run the focused tests and confirm missing notification behavior.
3. Add `sd-notify` at a reviewed locked version and send readiness only after
   successful bind; spawn watchdog notification only when systemd enables it.
4. Re-run focused tests, startup tests, Clippy, and `cargo deny check`.

### Task 4: Docker image and default contract

**Files:**
- Create: `Dockerfile`
- Create: `.dockerignore`
- Create: `packaging/docker/ttygate.toml`
- Create: `packaging/docker/README.md`

**Steps:**

1. Re-run `./scripts/check-packaging.sh` and capture its Docker failures.
2. Add digest-pinned Node, Rust, and Debian stages using locked dependency
   installs and a fixed non-root UID/GID.
3. Copy only the stripped daemon and required runtime packages; install the
   root-owned config and service-owned audit directory.
4. Declare the daemon health-check command and loopback-only default config.
5. Re-run the static checker until the Docker contract is green.

### Task 5: Docker smoke suite

**Files:**
- Create: `scripts/smoke-docker.sh`

**Steps:**

1. Write the smoke assertions before adding any accommodation: clean build,
   repeated-build identity, non-root UID/GID, minimal runtime contents,
   read-only root operation with explicit writable audit storage, internal
   loopback health, absence of default host port bindings, unsafe mount/permission
   rejection, and stop/failure process cleanup.
2. Run the smoke suite and confirm the first missing behavior fails with a
   useful non-secret label.
3. Make the smallest Docker/config changes needed for each assertion.
4. Re-run after each change and verify trap cleanup with Docker object and host
   process scans.

### Task 6: Hardened systemd package

**Files:**
- Create: `packaging/systemd/ttygated.service`
- Create: `packaging/systemd/ttygate.sysusers`
- Create: `packaging/systemd/ttygate.tmpfiles`
- Create: `packaging/systemd/ttygate.toml`
- Create: `packaging/systemd/README.md`

**Steps:**

1. Re-run `./scripts/check-packaging.sh` and capture the missing-unit failure.
2. Add the dedicated-account provisioning files and localhost-only config.
3. Add `Type=notify`, watchdog, cgroup cleanup, and the reviewed filesystem,
   home, device, namespace, capability, syscall, address-family, writable-path,
   and privilege restrictions.
4. Document why `PrivateNetwork` and `PrivateUsers` cannot safely be enabled.
5. Re-run the static checker and `systemd-analyze verify` on disposable Linux.

### Task 7: systemd smoke suite and CI

**Files:**
- Create: `scripts/smoke-systemd.sh`
- Modify: `.github/workflows/ci.yml`

**Steps:**

1. Add Linux smoke assertions for unit verification, localhost-only listening,
   readiness, watchdog activity/failure, restart, cgroup-wide child teardown,
   fail-closed permissions/config, effective hardening, and residue cleanup.
2. Run the smoke in its supported container/VM and confirm the first missing
   integration fails for the intended reason.
3. Add separate packaging CI jobs with pinned actions and Docker/systemd
   prerequisites; preserve useful bounded diagnostics as logs, not published
   artifacts.
4. Re-run YAML/static checks and both smoke suites.

### Task 8: Documentation reconciliation

**Files:**
- Modify: `README.md`
- Modify: `docs/roadmap.md`
- Modify: `docs/ttygate-rewrite-plan.md`
- Modify: `docs/threat-model.md`
- Modify: `scripts/check-public-docs.sh`

**Steps:**

1. Add public-doc assertions for Chunk 4.1 package paths, defaults, runtime
   identities, health/watchdog behavior, ownership, restrictions, limitations,
   and `Refs #12`.
2. Run the public-doc checker and confirm the new assertions fail.
3. Update only naturally stale packaging status and operator contract text;
   retain pre-release language and explicitly leave Chunks 4.2/4.3 incomplete.
4. Re-run public-doc and packaging checkers.

### Task 9: Full verification and publication

**Files:**
- Modify: this plan with exact evidence and residual limitations

**Steps:**

1. Run and inspect format, Clippy, all Rust tests with `--nocapture`, deny,
   frontend type/unit/build/e2e, public docs, packaging checker, Docker smoke,
   supported systemd verification/smoke, and
   `git diff --check origin/main...HEAD`.
2. Perform secret, private material, machine-path, generated-junk, process,
   Docker object, service, network, volume, and fixture residue scans.
3. Commit focused changes; every commit message includes `Refs #12`.
4. Push `feat/chunk-4.1-docker-systemd` and prove local/remote SHA equality.
5. Open a draft PR against `main` with `Refs #12`, read it back, and inspect
   every check and unresolved review thread to terminal state.
6. For verified failures/findings, add a RED regression, implement the minimum
   fix, commit with `Refs #12`, push, and wait again.
7. Repeat full residue verification, mark the PR ready, and read back final
   base/head/draft/SHA/check state without merging or closing issue #12.

## Verification evidence

Local verification on 2026-07-19:

- `cargo fmt --all --check` and warning-denying Clippy completed with no
  findings.
- `cargo test --workspace -- --nocapture` passed all 400 tests: 251 library,
  12 audit, 4 health-check, 41 HTTP, 11 PTY, 3 service-manager, 18 SSH, 16 TLS,
  20 trusted-proxy, and 24 WebSocket tests.
- `cargo deny check` reported advisories, bans, licenses, and sources as
  `ok`; its only output was the existing duplicate-version warnings for
  `getrandom`, `r-efi`, and `windows-sys`.
- Frontend type-check and build passed; all 47 unit tests and 4 Playwright
  browser tests passed.
- Packaging/public-documentation contract checks, POSIX syntax, ShellCheck,
  workflow YAML parsing, and diff whitespace checks passed.
- The Docker smoke passed two clean no-cache builds with identical OCI
  manifest `sha256:87cca1587b18a1b799480a499672fe965820236db913f49143b1100ee9262596`,
  then passed health, non-root/read-only/minimal-runtime, network, PTY teardown,
  fail-closed permission, diagnostic-secrecy, and object-cleanup assertions.
- The macOS host cannot run `systemd-analyze`; the script produced its explicit
  platform skip. The required unit verification and runtime lifecycle smoke
  run in the pull request's disposable Linux `systemd-packaging` job and must
  be green before the pull request is marked ready.

Residual limitations:

- `PrivateNetwork` cannot be enabled because the daemon must accept a listener
  and reach SSH targets. `PrivateUsers` cannot be enabled without invalidating
  the daemon's audit/SSH ownership checks. The package documents both explicit
  exceptions and compensating controls.
- The Docker default binds only inside container loopback and publishes no
  host port. Chunk 4.2, not this package, owns reviewed production binding and
  reverse-proxy examples.
- Release provenance, SBOM/checksum automation, image publication, and the
  v0.1 tag remain Chunk 4.3 work; this chunk publishes no artifact.
