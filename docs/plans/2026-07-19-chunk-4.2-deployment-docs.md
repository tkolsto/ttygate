# Chunk 4.2 Verified Deployment Documentation Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Deliver operator-readable Caddy and Nginx reverse-proxy deployments,
prove both through the real HTTPS/cookie/ticket/WSS/PTY/audit lifecycle, and
complete the Chunk 4.2 public documentation.

**Architecture:** Keep the two proxy configurations explicit while sharing one
disposable Docker harness, synthetic auth gateway, client lifecycle fixture,
ephemeral TLS setup, audit scanner, and cleanup contract. Pair dynamic proxy
smokes with fast static deployment/public-document checks and required
separate Linux CI jobs.

**Tech Stack:** POSIX shell, Docker, Caddy, Nginx, Node.js, OpenSSL,
Rust/ttygated, GitHub Actions, Markdown.

---

### Task 1: Deployment contract checker

**Files:**
- Create: `scripts/check-deployment-docs.sh`
- Modify: `.gitignore`

**Steps:**

1. Write assertions for the not-yet-present Caddy, Nginx, ttygate, guide,
   fixture, smoke, CI, README, roadmap, threat-model, and rewrite-plan
   contracts.
2. Assert HTTPS/public URL alignment, explicit identity replacement, missing
   authentication denial, WebSocket forwarding, backend isolation, exact
   trusted CIDR guidance, immutable proxy inputs, safe placeholders, audit
   responsibilities, non-goals, and Chunk 4.3 limitations.
3. Run `./scripts/check-deployment-docs.sh`.
4. Confirm RED because `packaging/reverse-proxy/Caddyfile` is absent.
5. Add only generated smoke paths to `.gitignore`; rerun and retain the
   expected missing-example failure.
6. Commit with `Refs #12`.

### Task 2: Operator-readable proxy and ttygate examples

**Files:**
- Create: `packaging/reverse-proxy/Caddyfile`
- Create: `packaging/reverse-proxy/nginx.conf`
- Create: `packaging/reverse-proxy/ttygate.toml`

**Steps:**

1. Use the Task 1 failures as the RED assertions.
2. Add a Caddy HTTPS listener using supplied certificate files,
   `forward_auth`, copied canonical identity, explicit authorization removal,
   upstream identity replacement, preserved Host, and native WebSocket
   forwarding.
3. Add an Nginx HTTPS listener and HTTP-to-HTTPS redirect using
   `auth_request`, `auth_request_set`, canonical identity replacement,
   authorization removal, preserved Host, explicit HTTP/1.1 WebSocket upgrade,
   and bounded proxy timeouts.
4. Add a production ttygate template with an external HTTPS `public_url`,
   plaintext private backend listener, exact trusted proxy CIDR marker,
   trusted-proxy provider, required audit sink, bounded limits, local PTY
   example, and strict-host-key SSH example.
5. Run the checker after each file and confirm only the next missing behavior
   fails.
6. Commit with `Refs #12`.

### Task 3: Operator deployment guide

**Files:**
- Create: `packaging/reverse-proxy/README.md`

**Steps:**

1. Re-run the deployment checker and capture the missing-guide RED failure.
2. Document replacement markers, secure invocation, ownership and modes,
   production certificate/secret handling, exclusive backend reachability,
   exact trusted CIDRs, public URL/Origin/cookie behavior, WebSockets,
   `/healthz`, rate-limit peer semantics, stable diagnostics, and
   Caddy/Nginx differences.
3. Add Cloudflare Access-style guidance requiring Tunnel validation or full
   JWT signature/claim validation rather than trusting an email header alone.
4. Add Tailscale Serve-style guidance using `Tailscale-User-Login`, loopback
   reachability, Serve header stripping, and Funnel/tagged-device limitations.
5. Document audit persistence, rotation, retention, shipping, confidentiality,
   and no terminal I/O; include primary official documentation links.
6. State proxy compromise, provider misconfiguration, shared OS-user, fixture
   certificate, and pre-release residual risks.
7. Re-run the checker.
8. Commit with `Refs #12`.

### Task 4: Shared auth and lifecycle fixtures

**Files:**
- Create: `scripts/fixtures/reverse-proxy-auth.mjs`
- Create: `scripts/fixtures/reverse-proxy-session.mjs`

**Steps:**

1. Add Node unit assertions for missing/invalid synthetic authorization denial,
   one canonical identity response, bounded inputs, safe errors, cookie/ticket
   handling, WebSocket ticket-first behavior, PTY echo, and secret-free
   diagnostics.
2. Run the focused Node tests and confirm RED because the fixtures do not
   exist.
3. Implement the smallest bounded auth server and HTTPS/WSS client needed by
   the harness.
4. Rerun the focused tests and adjacent frontend fixture tests.
5. Commit with `Refs #12`.

### Task 5: Disposable proxy smoke harness

**Files:**
- Create: `scripts/smoke-reverse-proxy.sh`

**Steps:**

1. Write the harness assertions before accommodations: tool availability,
   native proxy config validation, ephemeral certificate generation,
   isolated network, unpublished backend, HTTPS health/frontend, missing-auth
   denial, spoof replacement, secure cookie, real ticket/WSS/PTY echo,
   canonical audit chain, untrusted-peer rejection, fail-closed production
   matrix, secret scan, process teardown, and object/file cleanup.
2. Run `./scripts/smoke-reverse-proxy.sh caddy`; confirm the first missing
   lifecycle behavior fails with a non-secret label.
3. Implement only enough orchestration to make each Caddy assertion pass.
4. Run `./scripts/smoke-reverse-proxy.sh nginx`; confirm the first
   Nginx-specific behavior fails.
5. Implement only enough proxy-specific orchestration to make each Nginx
   assertion pass.
6. Exercise normal stop, daemon stop, proxy stop, assertion failure, and signal
   cancellation; verify no container, image, network, volume, certificate,
   log, process, or fixture survives.
7. Run ShellCheck and `sh -n`.
8. Commit with `Refs #12`.

### Task 6: README checklist and public documents

**Files:**
- Modify: `README.md`
- Modify: `docs/roadmap.md`
- Modify: `docs/ttygate-rewrite-plan.md`
- Modify: `docs/threat-model.md`
- Modify: `scripts/check-public-docs.sh`

**Steps:**

1. Add failing public-doc assertions for the production checklist, provider
   matrix, PTY/SSH examples, synthetic audit JSONL, Shell In A Box comparison,
   explicit non-goals, verified Caddy/Nginx examples, proxy residual risks,
   and remaining Chunk 4.3 gate.
2. Run `./scripts/check-public-docs.sh` and confirm the new RED failure.
3. Update README with the complete Chunk 4.2 checklist content and links to
   the detailed deployment guide.
4. Reconcile roadmap, rewrite-plan, and threat-model status without claiming
   M4, release, or production readiness.
5. Rerun public-doc and deployment checks.
6. Commit with `Refs #12`.

### Task 7: Required Caddy and Nginx CI jobs

**Files:**
- Modify: `.github/workflows/ci.yml`

**Steps:**

1. Add static checker assertions requiring separate proxy jobs, timeouts,
   pinned actions, and immutable Caddy/Nginx image references.
2. Run the deployment checker and confirm RED because the jobs are absent.
3. Add separate required disposable Linux jobs that build ttygate and execute
   the Caddy and Nginx smoke lifecycles without publishing artifacts.
4. Parse the workflow as YAML and run all static shell/document contracts.
5. Run both smoke jobs locally when Docker supports them; otherwise retain the
   explicit local skip and require green CI before claiming verification.
6. Commit with `Refs #12`.

### Task 8: Security review and negative findings

**Files:**
- Modify: only files implicated by verified findings

**Steps:**

1. Review TLS redirect/termination, backend exposure, actual socket-peer
   authority, source preservation, header deletion/duplication/normalization,
   WebSocket upgrade, Host/Origin, secure cookies, identity propagation,
   Cloudflare/Tailscale assumptions, proxy compromise, CIDR breadth and
   address families, audit confidentiality, key/config permissions,
   rate-limit attribution, Docker/systemd compatibility, diagnostics,
   cleanup, and supply-chain pins.
2. For each actionable finding, add a focused failing checker or smoke
   assertion.
3. Run it and verify RED for the intended reason.
4. Apply the smallest fix and verify GREEN plus adjacent checks.
5. Independently review the full diff because the user explicitly prohibited
   subagents.
6. Commit verified fixes with `Refs #12`.

### Task 9: Full verification and draft publication

**Files:**
- Modify: this plan with exact evidence and residual limitations

**Steps:**

1. Run and inspect `cargo fmt --all --check`,
   `cargo clippy --all-targets --all-features -- -D warnings`,
   `cargo test --workspace -- --nocapture`, and `cargo deny check`.
2. Run and inspect frontend type checks, unit tests, Playwright tests, and
   build.
3. Run public-doc, packaging, deployment, POSIX syntax, ShellCheck, Docker
   packaging, Caddy smoke, Nginx smoke, workflow YAML, and
   `git diff --check origin/main...HEAD`.
4. Record exact test summaries and any platform-only CI requirement in this
   plan; do not claim an unrun job passed.
5. Audit for local overrides, credentials, cookies, tickets, authorization,
   keys/certificates, real account/domain/CIDR/known-host data, absolute
   machine paths, generated files, processes, containers, images, networks,
   volumes, logs, and fixture directories.
6. Commit focused evidence changes with `Refs #12`.
7. Push `feat/chunk-4.2-deployment-docs` and prove local/remote SHA equality.
8. Open a draft PR to `main` titled
   `Chunk 4.2: add verified reverse-proxy deployment guides`, with `Refs #12`
   and no close keyword.
9. Read back the draft PR and verify base, head, title, body, and matching
   local/remote/PR SHAs. Do not merge or mark ready.
