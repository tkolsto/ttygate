# Strict-Host-Key OpenSSH Backend Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> to implement this plan task-by-task, and superpowers:test-driven-development
> for every production change.

**Goal:** Implement Roadmap Chunk 3.1 end to end so typed SSH targets use a
strict, server-constructed OpenSSH subprocess with safe admission, audit,
frontend, lifecycle, and real-sshd behavior (Refs #11).

**Architecture:** Prepare literal SSH executable, known-hosts, and identity
files before bind; resolve authenticated identity through the existing typed
user policies; build a fixed argv/environment; and run OpenSSH under the
existing PTY/process-group supervisor. A bounded stderr classifier gates
Running/audit admission so connection failures create one denial rather than a
false session lifecycle.

**Tech Stack:** Rust 2024, Tokio, pty-process, nix, axum WebSockets, serde/TOML,
TypeScript, Node test runner, Playwright/Chromium, Docker OpenSSH fixture.

**Design:** `docs/plans/2026-07-18-chunk-3.1-openssh-backend-design.md`

---

## Execution rules

- Work only on `feat/chunk-3.1-openssh-backend` from verified
  `7629344b7b85b043eb168a161acdebb788293262`.
- Add every named test before production code and inspect its feature-missing
  failure.
- Record RED output, GREEN output, and the implementing commit in the evidence
  table at the end of this file.
- Commit only coherent green slices and include `Refs #11`.
- Never commit temporary keys, known-hosts files, audit logs, containers,
  generated frontend output, machine paths, credentials, or local overrides.
- Review each task for spec compliance, then code quality. The final independent
  review is security-focused and repeats until it has no findings at any
  severity.

### Task 1: Typed credential and username contract

**Files:**
- Modify: `crates/ttygated/src/config.rs`
- Modify: `README.md` only for configuration fixture text required by public tests

**RED tests:**

- `ssh_target_requires_literal_executable_and_identity_paths`
- `ssh_target_rejects_unknown_duplicate_and_stray_credential_fields`
- `fixed_user_policy_resolves_exactly`
- `same_as_authenticated_user_policy_resolves_exactly`
- `mapping_user_policy_resolves_exactly_and_rejects_missing_mapping`
- `ssh_usernames_reject_option_control_whitespace_confusable_unicode_and_oversize`
- `ssh_literal_paths_preserve_bytes_without_tilde_or_environment_expansion`
- `unknown_target_fails_before_ssh_construction`

**Steps:**

1. Extend test TOML with required `ssh_executable` and `identity_file` fields;
   add the named rejection and resolution tables.
2. Run each exact test and inspect the compile/schema/validation failure.
3. Add the two fields to `SshTarget`/`RawTarget::Ssh`, keep
   `deny_unknown_fields`, and implement the closed ASCII username grammar.
4. Run exact tests, all `config::tests`, and adjacent allowlist/server tests.
5. Commit: `feat: type SSH credentials and user policy (Refs #11)`.

### Task 2: Fail-closed SSH material preparation before bind

**Files:**
- Create: `crates/ttygated/src/ssh.rs`
- Modify: `crates/ttygated/src/lib.rs`
- Modify: `crates/ttygated/src/startup.rs`
- Modify: `crates/ttygated/src/server.rs`
- Modify: `crates/ttygated/Cargo.toml` only if a focused parsing dependency is required

**RED tests:**

- `ssh_executable_rejects_relative_symlink_special_nonexecutable_and_writable_files`
- `known_hosts_rejects_relative_symlink_directory_special_wrong_owner_unsafe_empty_oversize_and_incomplete_data`
- `identity_rejects_relative_symlink_directory_special_wrong_owner_unsafe_empty_oversize_encrypted_and_trailing_data`
- `ssh_material_paths_are_rechecked_before_spawn`
- `ssh_capability_probe_uses_literal_executable_fixed_argv_and_cleared_environment`
- `ssh_preparation_failure_reaches_neither_application_nor_bind`
- `production_configured_ssh_target_has_no_insecure_runtime_fallback`

**Steps:**

1. Add Unix temporary-file tables and an injected capability-probe recorder.
2. Run exact tests and verify the preparation API is missing.
3. Implement no-follow opens, metadata/content/snapshot validation, safe error
   enums, and bounded direct `ssh -G` capability probing with a cleared
   environment.
4. Insert SSH preparation after config/TLS validation and before audit,
   application construction, or bind. Store only safe prepared metadata in
   application/session state.
5. Run exact tests, `config::tests`, `startup::tests`, TLS/audit filesystem
   suites, and `cargo clippy -p ttygated --all-targets -- -D warnings`.
6. Commit: `feat: prepare SSH policy inputs before bind (Refs #11)`.

### Task 3: Exact argv, environment, and diagnostic classifier

**Files:**
- Modify: `crates/ttygated/src/ssh.rs`
- Modify: `crates/ttygated/src/pty_backend.rs`

**RED tests:**

- `ssh_argv_serializes_every_pinned_option_exactly_once`
- `ssh_spawn_contract_uses_no_shell_and_only_the_literal_executable`
- `ssh_argv_loads_no_ambient_configuration`
- `ssh_argv_disables_password_keyboard_interactive_agent_x11_forwarding_proxy_and_local_commands`
- `ssh_argv_authority_comes_only_from_typed_target_and_resolved_policy`
- `hostile_browser_and_identity_values_cannot_alter_ssh_argv_structure`
- `ssh_environment_is_cleared_and_contains_only_fixed_locale_and_term`
- `ssh_classifier_distinguishes_unknown_mismatch_transport_authentication_and_generic_failure`
- `ssh_classifier_is_bounded_locale_pinned_and_never_returns_diagnostics`

**Steps:**

1. Add exact-vector, property/table-driven hostile value, environment recorder,
   and bounded transcript tests.
2. Run each exact test and inspect the missing builder/classifier failures.
3. Implement `PreparedSshTarget`, direct `pty_process::Command`, the fixed argv,
   separate piped stderr, environment clearing, and closed diagnostic classes.
4. Run exact tests plus all `ssh`, `pty_backend`, config, protocol, and secrecy
   unit suites.
5. Commit: `feat: construct the strict OpenSSH subprocess (Refs #11)`.

### Task 4: Supervised connecting-to-running admission

**Files:**
- Modify: `crates/ttygated/src/session.rs`
- Modify: `crates/ttygated/src/pty_backend.rs`
- Modify: `crates/ttygated/src/audit.rs`

**RED tests:**

- `ssh_policy_denial_happens_before_spawn_and_releases_capacity`
- `ssh_host_key_failure_has_one_denial_and_no_session_lifecycle`
- `ssh_connection_failure_has_one_denial_and_no_session_lifecycle`
- `ssh_authentication_failure_has_one_denial_and_no_session_lifecycle`
- `ssh_generic_setup_failure_has_one_denial_and_no_session_lifecycle`
- `ssh_admission_emits_created_running_and_one_correlated_start_only_after_authentication`
- `cancelled_ssh_setup_is_registered_terminated_reaped_and_releases_capacity`
- `shutdown_during_ssh_setup_terminates_reaps_and_releases_capacity`
- `audit_failure_during_ssh_setup_terminates_and_reaps_without_authority`
- `ssh_remote_exit_status_is_not_connection_setup_failure`
- `existing_local_pty_lifecycle_is_unchanged`

**Steps:**

1. Add an injected backend with controllable admission and child-reap signals;
   add exact audit serialization variants.
2. Run each exact test and verify current SSH targets are unavailable or local
   spawn is incorrectly treated as Running.
3. Generalize the backend target contract, register the connecting supervisor
   before awaiting admission, gate lifecycle/audit start on authentication, and
   map each pre-admission class to one stable denial.
4. Preserve reservation ownership through cleanup and all unwind/drop paths.
5. Run exact tests, all session/audit/PTY suites, and 20 in-process admission
   cancellation/shutdown iterations.
6. Commit: `feat: gate SSH lifecycle on authenticated admission (Refs #11)`.

### Task 5: WebSocket and frontend stable SSH states

**Files:**
- Modify: `crates/ttygated/src/websocket.rs`
- Modify: `crates/ttygated/src/protocol.rs` if shared fixtures require additions
- Modify: `frontend/src/protocol.ts`
- Modify: `frontend/src/connection.ts`
- Modify: `frontend/src/connection.test.ts`
- Modify: `frontend/src/ui.ts`
- Modify: `frontend/src/ui.test.ts`
- Modify: `docs/protocol.md`
- Modify: `shared/protocol-fixtures.json` if present

**RED tests:**

- `websocket_admits_configured_ssh_targets_without_new_authority_fields`
- `websocket_maps_unknown_and_mismatched_keys_to_safe_host_key_failure`
- `websocket_distinguishes_ssh_connection_authentication_policy_and_generic_failures`
- `ssh_frontend_errors_never_reflect_target_host_user_path_argv_or_diagnostics`
- `frontend_exposes_stable_distinct_ssh_failure_states`
- `hostile_protocol_values_cannot_select_any_ssh_argument`

**Steps:**

1. Add Rust bridge and TypeScript state/copy tests first.
2. Run focused Rust and frontend tests; inspect current target rejection and
   unknown-code failures.
3. Permit typed SSH targets after ticket redemption, map only closed
   `SessionError` variants to curated protocol errors, and add frontend states.
4. Keep the generic protocol envelope unchanged and document the fixed codes.
5. Run WebSocket, protocol, frontend unit/type/build, and existing authority
   suites.
6. Commit: `feat: surface safe SSH connection states (Refs #11)`.

### Task 6: Real containerized sshd integration and cleanup

**Files:**
- Create: `crates/ttygated/tests/ssh_server.rs`
- Create: `crates/ttygated/tests/fixtures/sshd/Dockerfile`
- Create: `crates/ttygated/tests/fixtures/sshd/entrypoint.sh`
- Modify: test support modules only where reuse is safe

**RED tests:**

- `strict_known_host_ssh_reaches_real_sshd`
- `empty_unknown_known_hosts_rejects_before_session_start`
- `mismatched_host_key_has_stable_frontend_and_audit_failure`
- `refused_port_is_distinct_connection_failure`
- `wrong_identity_key_is_distinct_authentication_failure`
- `remote_exit_status_propagates_after_admission`
- `ssh_resize_reaches_remote_pty`
- `ssh_terminal_input_output_are_opaque_and_absent_from_audit`
- `ssh_secret_sentinels_never_reach_audit_or_frontend_errors`
- `ssh_websocket_drop_terminates_and_reaps_local_process`
- `ssh_idle_absolute_explicit_cancellation_shutdown_and_unwind_complete_once`
- `resistant_ssh_cleanup_holds_capacity_until_group_reaped`
- `sshd_has_no_lingering_session_child_after_cleanup`

**Steps:**

1. Build an always-cleaning Docker fixture using generated temporary host/client
   keys and known-host entries provisioned from generated public data.
2. Add all named tests and run one strict-success test to observe the
   feature/fixture missing RED.
3. Implement only fixture support needed to exercise the production path.
4. Run every exact case, complete `ssh_server`, existing PTY/WebSocket/audit
   suites, and container-side process inspection.
5. Commit: `test: prove strict OpenSSH behavior against real sshd (Refs #11)`.

### Task 7: Independent security review and regression loop

**Files:**
- Modify only files required by validated findings
- Modify: this plan's evidence/disposition tables

**Review scope:**

- shell/argument/option injection through every configured or protocol field;
- ambient config, identity, agent, proxy, forwarding, command, and environment;
- host-key downgrade/learning;
- executable/known-host/identity ownership, permission, symlink, and race checks;
- credential, diagnostic, terminal, cookie, ticket, header, and audit leakage;
- OpenSSH version/locale classification;
- false lifecycle success, duplicate audit, ticket/capacity races;
- PTY/process-group cleanup and residual remote-process risk;
- frontend reflection and documentation truth.

**Steps:**

1. Dispatch an independent security-focused reviewer against
   `origin/main..HEAD`.
2. Record every finding in the disposition table.
3. For each valid finding, add and run a named failing regression.
4. Apply the smallest fix, rerun focused and adjacent suites, and commit
   `fix: address OpenSSH security review findings (Refs #11)`.
5. Re-review with a fresh independent reviewer until Critical, Important, and
   Minor sections are all empty.

### Task 8: Public documentation and M3 truth

**Files:**
- Modify: `README.md`
- Modify: `docs/roadmap.md`
- Modify: `docs/ttygate-rewrite-plan.md`
- Modify: `docs/threat-model.md`
- Modify: `docs/protocol.md`
- Modify: `scripts/check-public-docs.sh`
- Modify: this plan

**RED tests:**

- public-doc assertions for the exact configuration and pinned policy;
- public-doc assertions for credential/known-host provisioning and literal
  filesystem trust;
- public-doc assertions for safe errors/audit and cleanup limits;
- public-doc assertions for OpenSSH compatibility and all deferred scope;
- stale-planning assertions that reject describing SSH as unimplemented.

**Steps:**

1. Add public-document checks before changing prose and run them to failure.
2. Update configuration examples, status, threat mitigations, compatibility,
   operational responsibilities, residual remote risk, and deferred scope.
3. Mark Chunk 3.1/M3 complete only after Task 6 and review evidence are green.
4. Keep the pre-release warning accurate.
5. Run public-doc checks and `git diff --check`.
6. Commit: `docs: document strict OpenSSH targets (Refs #11)`.

### Task 9: Full verification and publication

**Verification commands:**

```text
cargo fmt --all --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --workspace -- --nocapture
cargo deny check
cd frontend && npm run check
cd frontend && npm test
cd frontend && npm run build
cd frontend && npm run test:e2e
<real sshd integration command>
./spikes/openssh/run.sh
./scripts/check-public-docs.sh
git diff --check origin/main...HEAD
```

Run focused strict-host-key, mismatch, connection, authentication, user-policy,
argv-injection, secrecy, lifecycle, and authority regressions separately.
Repeat the host-key mismatch classification/cleanup exact test in 20 independent
processes. Repeat one race-sensitive SSH cancellation/cleanup exact test in 20
independent processes.

Inspect `git diff --stat`, `git diff --name-status`, and the full diff. Scan for
local overrides, credentials, private keys, known-host data, generated
artifacts, absolute machine paths, test secrets, forbidden SSH fields, and
sensitive sentinels. Inspect processes and Docker for surviving ttygated, ssh,
sshd session, PTY, proxy, WebSocket, Playwright, Chromium, or fixture
containers.

Record exact counts and outputs below, commit only the evidence update with
`Refs #11`, push exact HEAD, verify the remote SHA, create the required draft PR,
and read it back. Do not merge.

## TDD evidence and requirement disposition

| Requirement/slice | RED command and observed feature-missing failure | GREEN command and result | Commit |
|---|---|---|---|
| Baseline from verified `origin/main` | N/A | `cargo test --workspace -- --nocapture`: 299 passed; frontend check, 44 tests, and build passed | `7629344` |
| Typed credential and user policy | Module-wide compile RED: every named-test filter compiled the complete config test module and failed with E0609 because `SshTarget` had no `ssh_executable` or `identity_file`; those failures independently proved only the missing typed fields. A later mutation replay of the old permissive username validator made `ssh_usernames_reject_option_control_whitespace_confusable_unicode_and_oversize` fail by accepting `operator-`. The policy-resolution and unknown-target cases are characterization/adjacent guards, not independent feature-missing RED. | With the explicit stable-toolchain `PATH`: all eight original exact `--lib` tests passed; `cargo test -p ttygated --lib config::tests::` passed 40; `cargo test -p ttygated --lib allowlist` passed 4; escalated `cargo test -p ttygated` passed 307 total tests (local socket bind requires sandbox escalation) | `e42f448` (`feat: type SSH credentials and user policy (Refs #11)`) |
| Pre-bind file/capability validation | Initial RED was one shared module-wide compile block, not seven independent behavioral REDs: each of the seven named exact commands exited 101 with E0432/E0425/E0599 because the SSH preparation API/constants and `StartupError::Ssh` did not exist. Independent self-review REDs then failed 0/1 as intended: `ssh_capability_probe_uses_literal_executable_fixed_argv_and_cleared_environment` returned `CapabilityUnsupported` for OpenSSH's valid `pubkeyauthentication true`; `identity_rejects_relative_symlink_directory_special_wrong_owner_unsafe_empty_oversize_encrypted_and_trailing_data` unexpectedly accepted an incomplete envelope; and `ssh_material_paths_are_rechecked_before_spawn` unexpectedly accepted a material swap during the probe. Spec review added two independent REDs against `0177431`: `capability_probe_covers_and_validates_complete_strict_runtime_vocabulary` failed 0/1 because the probe lacked the complete strict option vocabulary and `--`; `identity_parser_rejects_malformed_ed25519_structure` failed 0/1 because the structural parser returned `Ok` for unsupported/malformed key data. Quality review produced behavioral REDs for live timeout/flood children, accepted non-aligned padding, accepted duplicate/unsafe proxy transcripts, missing checked-add bounds, unprepared production construction, and mismatched prepared maps reaching bind. | All original, spec-review, and quality-review regressions passed. With the explicit stable-toolchain `PATH`, escalated `cargo test -p ttygated --quiet` passed 327 total tests with zero failures. `cargo clippy -p ttygated --all-targets -- -D warnings`, `cargo fmt --all -- --check`, and `git diff --check` passed. | `0177431` (`feat: prepare SSH policy inputs before bind (Refs #11)`); `fix: enforce complete SSH preparation policy (Refs #11)`; `fix: harden SSH preparation cleanup and invariants (Refs #11)` |
| Exact argv/environment/classifier | All nine original named tests were added before production changes. Initial builder/classifier compile RED and the later material-swap, logical-line, live-classification, invalid-UTF-8, admission-channel, authority-binding, and hostname/failure-grammar REDs are retained in the Task 3 review rows below. | All original named tests and review regressions pass. Admission uses only the private `-E` FIFO; prepared authority owns a closed canonical SSH host; success and failure classes require complete supported lines; and policy/spawn/material errors remain distinct and redacted. Final verification passed the exact admission fixture 10/10, the complete SSH module 3/3 (90/90 aggregate), four consecutive full-crate runs 4/4 (1,376/1,376 aggregate), Clippy all-targets with warnings denied, formatting, and diff check. Immutable spec and quality re-reviews at `2cad2f9` found zero Critical, Important, or Minor findings. | `7b8d6d3`; `3dcad66`; `443de3d`; `5547033`; `c84cdb9`; `9d46049`; `8a3c91d`; `4c6208d`; `2cad2f9` |
| Admission/audit/lifecycle | All eleven named Task 4 tests were added before production changes. The first exact filter exited 101 with E0433/E0599 because the injected admission fixture/seam and closed SSH `SessionError` vocabulary did not exist; the module-wide compile failure independently established the missing Task 4 contract. Review then added `ssh_pre_spawn_build_failure_has_one_denial_and_no_session_lifecycle`; its exact RED exited 101/E0599 because the fixture had no material-invalidation seam. The reviewer also observed the old `AtomicUsize`/`Notify` fixture hanging at 49/50 tests for more than 90 seconds, directly demonstrating its lost-wakeup race. The quality-review unwind/quarantine regressions first exited 101 with E0433/E0599 because no connecting panic point, panic-enabled manager, or blocked-cleanup proof seam existed. The bounded-reap regression failed 0/1 because post-SIGKILL `Child::wait` exceeded its 300 ms deadline; the later after-take panic regression failed 0/1 when the child-reap proof timed out. Restoring the original local-PTY fixture semantics then made both natural resistant-group regressions fail at their three-second close deadlines. | All original named tests and review regressions pass. `session::tests` passed 10 independent complete-module runs (590/590 aggregate). Forced and natural resistant leader/descendant/PGID absence and capacity regressions each passed 20 independent processes. Natural exit is observed through `waitid` with `WNOWAIT`; deterministic tests prove both natural and requested teardown signal the pinned group before leader reap, then use probe-only ESRCH proof. Connecting and handoff owners remain unwind-recoverable through worker and supervisor construction. The final natural-exit remediation passed three consecutive complete workspace runs of 369/369 each (1,107/1,107 aggregate), Clippy on every target with warnings denied, formatting, diff checks, and `cargo deny check`. Fresh immutable spec and quality reviews at `c7a889a` found zero Critical, Important, or Minor findings. | `56fac08`; `d3fafd1`; `be5ed24`; `1dd164d`; `ad1c8f9`; `3a099fc`; evidence head `c7a889a` |
| WebSocket/frontend states | All six named tests were added before production changes. The first exact Rust filter exited 101/E0432 because no redeemed-target support gate existed. The focused frontend run failed `frontend_exposes_stable_distinct_ssh_failure_states` with `internal-error` instead of `ssh-host-key-failed`, and failed `ssh_frontend_errors_never_reflect_target_host_user_path_argv_or_diagnostics` because the SSH copy was absent. `hostile_protocol_values_cannot_select_any_ssh_argument` already passed as a characterization of the closed generic protocol schema. Quality review then replaced the weak direct-predicate proof with a controlled prepared-SSH WebSocket regression. With the production branch temporarily restored to PTY-only, that exact test exited 101 because it received `session-denied` instead of the expected curated `ssh-host-key-failed`, proving the regression exercises the real post-redemption gate. | The three exact Rust WebSocket tests passed 3/3; the complete WebSocket module passed 16/16 and protocol module 6/6. All three exact frontend tests passed; the complete frontend suite passed 47/47, TypeScript check and production build passed. The escalated real WebSocket bridge passed 24/24. The strengthened test redeems a ticket, sends only the unchanged ticket envelope, reaches the prepared SessionManager SSH path, observes one curated host-key failure, and proves exactly one manager-owned denial with no lifecycle or host/user/path/argv/diagnostic reflection. Unknown and mismatched keys share one safe host-key code; connection, authentication, policy, and generic setup failures remain distinct. Immutable spec and quality/security re-reviews at `0c5c2c9` found zero Critical, Important, or Minor findings. | `f1f8dc2` (`feat: surface safe SSH connection states (Refs #11)`); `bca52b0` (`test: prove WebSocket reaches prepared SSH (Refs #11)`); evidence head `0c5c2c9` |
| Real sshd and cleanup | All 13 named Task 6 cases plus `malicious_pre_auth_banner_cannot_forge_ssh_admission` were added before fixture support. The first exact strict-success run exited 101/E0433 because `RealSshdFixture` did not exist. The first real mismatch run returned `ssh-failed`, proving the classifier did not recognize installed OpenSSH 10.2's decorated changed-host warning. The first 20-process cancellation campaign then exposed a fixture-readiness RED on repetition 20: Docker had published the port before sshd accepted connections, so the first admission returned `SshConnectionFailed`. Review REDs then proved the missing WebSocket denial observer at compile time, missing sentinel-bearing materials at runtime, zero cancellation audit events, and a parallel fixture-construction timeout. Final reviews proved that the claimed argv/environment sentinels were not actual process inputs, the malicious banner was not an accepted classifier line, secret scans could render their values on failure, and the Docker fixture mounted every client secret into an unpinned, broadly privileged container. | The disposable Alpine/OpenSSH fixture generates fresh host, accepted client, rejected client, sentinel-bearing rejected client, and wrong-host Ed25519 keys under ignored `target/ssh-fixtures`. Client-only material and server-only material are separate 0700 directories; the read-only server mount contains exactly the host private key, accepted client public key, and banner. The Alpine multi-architecture digest and package versions are pinned; the captured build result must be an immutable sha256 image ID and both sshd and relay run that ID. sshd has a read-only root, minimal tmpfs, no-new-privileges, all capabilities dropped except the experimentally required CHOWN/SETGID/SETUID/SYS_CHROOT set, a PID limit, and an exclusively internal network with no default route. A mount-free, capability-free, read-only relay alone is dual-homed to publish the random loopback port. Cleanup owns relay, sshd, network, material, then serial lock. The complete real suite passed 17/17. Exact dynamic argv and isolated ambient-environment secrecy are non-vacuous; labeled scans never render values. The exact accepted classifier line in the pre-auth banner produces one authentication denial and zero lifecycle, while a private unit oracle proves the same raw-stderr bytes would authenticate if incorrectly trusted and the FIFO-fed classifier remains pending then denies. The banner regression passed 20/20 fresh processes. `cargo test -p ttygated -- --nocapture` passed 390/390; Clippy all-targets/all-features with warnings denied, formatting, diff check, and cargo-deny passed; final Docker/network/material/local-process and sentinel scans were empty. | `f94dd9d`; `9e879a8`; `032c786`; `ff305da` (`test: harden real SSH fixture evidence (Refs #11)`) |
| Review regressions | Task 6's final immutable spec and quality reviews initially found the argv/environment, banner, fixture-mount, secret-diagnostic, and container-runtime gaps retained below. | Every accepted Task 6 regression is green. Both final immutable re-reviews at `6991a34` reported zero Critical, Important, or Minor findings. | Evidence head `6991a34` |
| Public documentation | Pending | Pending | Pending |
| Final verification/repetitions/scans | Pending | Pending | Pending |

## Independent review findings

| Review | Severity | Finding | Disposition | Named RED regression | Fix commit | Re-review |
|---|---|---|---|---|---|---|
| Task 1 quality review | Important | The Task 1 evidence row incorrectly described eight independent feature-missing RED failures even though Cargo compiled the whole config test module before applying each filter. | Corrected the row to distinguish the module-wide missing-field compile RED, the reproduced old-validator behavioral RED, and characterization/adjacent guards. | N/A (evidence-only correction; no regression required) | `test: tighten SSH config review evidence (Refs #11)` | Resolved at `53f4e76`: no Critical, Important, or Minor findings; fresh fmt, both exact regressions, config 42, allowlist 5, clippy all-targets, full crate 309, and diff check passed. |
| Task 1 quality review | Minor | Same-as-authenticated-user runtime resolution lacked direct hostile identity coverage. | Added `same_as_authenticated_user_policy_rejects_hostile_runtime_identities`. Mutation RED: with the old permissive resolved-user validator restored temporarily, with the explicit stable-toolchain `PATH`, `cargo test -p ttygated --lib config::tests::same_as_authenticated_user_policy_rejects_hostile_runtime_identities -- --exact` failed because it accepted confusable Unicode `operаtor`. GREEN: the same exact command passed after restoring the strict validator. | `same_as_authenticated_user_policy_rejects_hostile_runtime_identities` | `test: tighten SSH config review evidence (Refs #11)` | Resolved at `53f4e76`: no Critical, Important, or Minor findings; fresh fmt, both exact regressions, config 42, allowlist 5, clippy all-targets, full crate 309, and diff check passed. |
| Task 1 quality review | Minor | Typed `SshTarget` credential-path validation branches were masked by an invalid-port allowlist fixture. | Added `allowlist_rejects_invalid_typed_ssh_credential_paths` with port 22 and independent invalid executable/identity paths. Mutation RED: after temporarily removing both typed credential-path validations, with the explicit stable-toolchain `PATH`, `cargo test -p ttygated --lib config::tests::allowlist_rejects_invalid_typed_ssh_credential_paths -- --exact` failed because `~/bin/ssh` produced an `Ok` allowlist; removing only identity-file validation made the same command fail because `$HOME/id_ed25519` produced `Ok`. GREEN: the same exact command passed after restoring both validations. | `allowlist_rejects_invalid_typed_ssh_credential_paths` | `test: tighten SSH config review evidence (Refs #11)` | Resolved at `53f4e76`: no Critical, Important, or Minor findings; fresh fmt, both exact regressions, config 42, allowlist 5, clippy all-targets, full crate 309, and diff check passed. |
| Task 2 self-review | Minor | Capability parsing treated OpenSSH's normalized boolean `pubkeyauthentication true` as unsupported even though it is equivalent to the requested `yes`. | Accepted OpenSSH's equivalent `yes`/`true` and `no`/`false` forms for the then-probed strict subset, including both known-host files. RED: `ssh_capability_probe_uses_literal_executable_fixed_argv_and_cleared_environment` failed 0/1 with `CapabilityUnsupported`. GREEN: the same exact test passed 1/1. | `ssh_capability_probe_uses_literal_executable_fixed_argv_and_cleared_environment` | `0177431` | Resolved at `0177431` for boolean normalization; later spec review found the option vocabulary incomplete and tracks that separately below. |
| Task 2 self-review | Important | The initial OpenSSH envelope check proved `ciphername = none` but did not reject an incomplete outer private-key structure. | Required exactly one public/private entry, complete outer framing, matching private check integers, a nonempty key type, and no outer trailing bytes. RED: `identity_rejects_relative_symlink_directory_special_wrong_owner_unsafe_empty_oversize_encrypted_and_trailing_data` failed 0/1 because preparation unexpectedly returned `Ok` for the incomplete envelope. GREEN: the same exact test passed 1/1. | `identity_rejects_relative_symlink_directory_special_wrong_owner_unsafe_empty_oversize_encrypted_and_trailing_data` | `0177431` | Resolved at `0177431`; the exact regression, SSH 6, clippy, formatting, diff check, and full crate 316 all passed. |
| Task 2 self-review | Important | Prepared material was exposed for later pre-spawn recheck, but a path replacement during the startup capability probe was not rejected before preparation returned. | Rechecked all three literal-path snapshots immediately before and after the capability probe. RED: `ssh_material_paths_are_rechecked_before_spawn` failed 0/1 because preparation unexpectedly returned `Ok` after the injected probe replaced `known_hosts`. GREEN: the same exact test passed 1/1. | `ssh_material_paths_are_rechecked_before_spawn` | `0177431` | Resolved at `0177431`; the exact regression, SSH 6, clippy, formatting, diff check, and full crate 316 all passed. |
| Task 2 spec review | Important | The capability probe covered only a strict subset and therefore did not prove support for the complete runtime option vocabulary or the fixed `--` target boundary. | Added one reusable ordered 28-option source of truth, fixed `-G -F /dev/null` framing, `--` plus a fixed target, exact-once recorder coverage, and canonical output validation for every security-relevant value. RED: `capability_probe_covers_and_validates_complete_strict_runtime_vocabulary` failed 0/1 because argv ended with `GlobalKnownHostsFile=/dev/null` plus the target instead of `--` plus the target. GREEN: the same exact test passed 1/1. | `capability_probe_covers_and_validates_complete_strict_runtime_vocabulary` | `fix: enforce complete SSH preparation policy (Refs #11)` | Resolved at `2d2b86a`: spec re-review found the implementation compliant and no Critical, Important, or Minor findings remained. |
| Task 2 spec review | Important | The identity parser accepted arbitrary synthetic remainder after a nonempty key type rather than structurally validating one supported unencrypted Ed25519 envelope. | Narrowed support to one unencrypted OpenSSH `ssh-ed25519` envelope and consumed the outer public blob and private block, exact lengths, matching check integers and public-key copies, embedded public half, bounded comment, canonical nonempty padding, and no trailing bytes. Cryptographic seed-to-public derivation was rejected as out of scope: ttygate validates envelope structure and delegates credential parsing and cryptographic validity to OpenSSH. RED: `identity_parser_rejects_malformed_ed25519_structure` failed 0/1 because unsupported/malformed structure returned `Ok`. GREEN: the same exact test passed 1/1 with a structurally valid non-secret synthetic fixture and a malformed table. | `identity_parser_rejects_malformed_ed25519_structure` | `fix: enforce complete SSH preparation policy (Refs #11)` | Resolved at `2d2b86a`: spec re-review found the structural-only boundary compliant; Ed25519 seed-to-public derivation was accepted as out of scope, and no Critical, Important, or Minor findings remained. |
| Task 2 quality review | Important | Probe timeout and output-budget failures relied on drop behavior and could leave a real child alive. | Added explicit kill plus await/reap cleanup on all early probe failures. Real shell regressions record the child PID, require bounded return, then prove the PID is absent and already reaped. Both tests failed with a live PID before the fix and pass afterward. | `capability_probe_timeout_kills_and_reaps_child`; `capability_probe_output_limit_kills_and_reaps_flooding_child` | `fix: harden SSH preparation cleanup and invariants (Refs #11)` | Resolved at `666b862`: the accepted production finding was fixed; final quality re-review found no Critical, Important, or Minor findings. |
| Task 2 quality review | Important | Cipher-none private-block padding was accepted without OpenSSH's exact 8-byte block alignment and 1-through-8 canonical padding rule. | Required the complete private block to be 8-byte aligned and padding to be exactly `01`, `01 02`, … through `01..08`. The malformed aligned/padding table failed before the fix and passes afterward. | `cipher_none_padding_requires_exact_aligned_openssh_block` | `fix: harden SSH preparation cleanup and invariants (Refs #11)` | Resolved at `666b862`: the accepted production finding was fixed; final quality re-review found no Critical, Important, or Minor findings. |
| Task 2 quality review | Important | Probe output matching did not reject duplicate/contradictory keys or robustly prove alias and proxy dispositions. | Parse output into a unique canonical key map; reject every duplicate; validate all 28 requested dispositions through 25 canonical values, the ChallengeResponse alias represented by `kbdinteractiveauthentication=no`, and canonical absence of `proxycommand`/`proxyjump`. Added hostile non-none proxy transcript coverage and a real `/usr/bin/ssh` capability check when installed. | `capability_probe_rejects_duplicate_contradictory_and_unsafe_proxy_transcripts`; `installed_openssh_accepts_required_capability_policy_when_available` | `fix: harden SSH preparation cleanup and invariants (Refs #11)` | Resolved at `666b862`: the accepted production finding was fixed; final quality re-review found no Critical, Important, or Minor findings. |
| Task 2 quality review | Minor | SSH string slicing used unchecked index addition and lacked a maximal length-prefix regression. | Centralized checked index addition and added a maximal `u32` prefix case plus an explicit `usize::MAX` overflow assertion. The regression first failed to compile because the checked helper did not exist, then passed after the fix. | `ssh_string_maximal_length_prefix_uses_checked_bounds` | `fix: harden SSH preparation cleanup and invariants (Refs #11)` | Resolved at `666b862`: the accepted production finding was fixed; final quality re-review found no Critical, Important, or Minor findings. |
| Task 2 quality review | Important | Prepared SSH material could be attached as an empty or mismatched map, and ordinary production constructors silently defaulted it. | Enforce exact equality between configured SSH allowlist names and prepared-map keys before bind; production constructors reject SSH targets lacking prepared startup material; the only empty constructor is explicitly test-named. Injected tests prove mismatch reaches no bind and exact map transfer follows TLS → SSH → audit → app → bind. | `ssh_prepared_map_mismatch_reaches_no_bind`; `production_app_construction_rejects_unprepared_ssh_allowlist`; `startup_orders_ssh_preparation_and_transfers_exact_map_before_bind` | `fix: harden SSH preparation cleanup and invariants (Refs #11)` | Resolved at `666b862`: the accepted production finding was fixed; final quality re-review found no Critical, Important, or Minor findings. |
| Task 2 quality review | Important | Filesystem and local-process documentation overstated symlink protection and did not state the same-UID credential boundary; the pinned runtime vector omitted the global known-hosts override. | Clarified that `O_NOFOLLOW` covers only the final component, made administrator-controlled parent namespaces explicit, added `GlobalKnownHostsFile=/dev/null`, and documented that same-daemon-UID PTY targets can read daemon-readable identities and are not OS-isolated. | N/A (documentation truth correction) | `fix: harden SSH preparation cleanup and invariants (Refs #11)` | Resolved at `666b862`: the accepted documentation finding was fixed; final quality re-review found no Critical, Important, or Minor findings. |
| Task 2 quality re-review | Minor | Real hanging/flood probe fixtures could run concurrently with ordinary and installed-client probe tests, consuming enough CPU/output to make a correct probe miss the shared two-second production deadline; the reviewer reproduced 13/14 followed by a green rerun. | Added one Tokio async mutex used only by the six tests that spawn real capability-probe child processes; pure parser and injected-preparation tests remain concurrent. All six affected exact tests passed, then the complete 14-test SSH module passed 10/10 sequential repetitions in ten independent Cargo processes (140/140 aggregate). | Reviewer's focused 13/14 failure is the RED evidence; the isolated real-process tests and repeated SSH module are the GREEN regression evidence. | `8a7ff85` (`test: isolate SSH capability probe fixtures (Refs #11)`) | Resolved at `8a7ff85`: final verification passed the SSH module in 10 independent Cargo processes (140/140), the full crate with 327 tests, clippy all-targets with warnings denied, formatting, and diff check; no Critical, Important, or Minor findings remained. |
| Task 3 self-review | Important | The first spawn seam rechecked prepared material only while constructing argv, leaving a replacement window before the PTY child was created. | Retained the prepared snapshots inside the redacted spawn specification and rechecked them immediately in the direct-spawn entry point before allocating/spawning the child. | `ssh_spawn_rechecks_material_after_argv_construction` | `feat: construct the strict OpenSSH subprocess (Refs #11)` | Resolved locally: the focused regression, SSH 24/24, and full 337-test crate verification passed. |
| Task 3 self-review | Minor | The incremental diagnostic line bound counted newline bytes instead of logical lines, allowing a nonempty 129th line after 128 completed lines. | Track blank and nonblank logical-line openings across chunks and fail closed before retaining/classifying a line beyond the fixed bound. | `ssh_classifier_is_bounded_locale_pinned_and_never_returns_diagnostics` | `feat: construct the strict OpenSSH subprocess (Refs #11)` | Resolved locally: the focused regression, SSH 24/24, Clippy with warnings denied, and full 337-test crate verification passed. |
| Task 3 spec review | Important | The classifier initially exposed a result only through consuming `finish()`, so Task 4 could not observe successful authentication or terminal setup failures while the live OpenSSH stderr stream remained open. First review follow-up found that the new live API also treated irrecoverable invalid UTF-8 as pending until EOF. | Added repeatable, non-consuming `classification() -> Option<SshDiagnosticClass>`. Complete supported markers are observable before EOF; partial markers, actually incomplete UTF-8, and unknown well-formed setup output remain pending; irrecoverable UTF-8 and bounds fail closed immediately; consuming `finish()` maps all remaining pending state to `GenericFailure`. Later mismatch evidence retains precedence over an earlier authenticated marker, and no raw diagnostic bytes enter results or `Debug`. Initial compile RED: E0599 for missing `classification()`. Follow-up behavioral RED: the exact regression failed 0/1 because `[0xff]` returned `None`; GREEN: the same test passed 1/1 using `Utf8Error::error_len()` to distinguish incomplete from irrecoverable input. | `ssh_classifier_exposes_live_setup_classification_before_eof` | `3dcad66` (`fix: expose live SSH setup classification (Refs #11)`); `443de3d` (`fix: reject invalid live SSH diagnostics (Refs #11)`) | Resolved at `443de3d`; immutable spec and quality re-reviews at `2cad2f9` found no remaining findings. |
| Task 3 quality review | Critical | Raw child stderr was used as the admission channel, allowing remote/banner-controlled bytes to imitate a success marker. | Added server-generated `-E` FIFO admission, separate raw-stderr discard ownership, private modes, readiness-based delayed-writer reads, bounded flood behavior, and cleanup proof. Capability probe now includes `-E /dev/null`. Real malicious-sshd banner coverage is explicitly queued for Task 6 and is not claimed here. | `ssh_admission_uses_private_fifo_and_never_raw_stderr`; `ssh_client_log_fifo_flood_is_incrementally_drained_and_bounded`; `capability_probe_covers_and_validates_complete_strict_runtime_vocabulary` | `5547033` (`fix: separate trusted SSH admission diagnostics (Refs #11)`) | Resolved at `5547033`; final re-review found only the hostname and failure-line findings tracked below. |
| Task 3 quality review | Important | Prepared authority omitted host, port, policy, and read-only state; builder accepted a second mutable target and policy denial was not distinct. | Prepared targets capture and compare the complete validated target, expose redacted Debug only, and are the sole argv authority. Missing/invalid mappings return `PolicyDenied`. | `prepared_ssh_authority_is_complete_redacted_and_policy_denial_is_exact` | `5547033` | Resolved at `5547033`; final re-review found only the hostname and failure-line findings tracked below. |
| Task 3 quality review | Important | Fallible stderr/PID extraction after spawn could return without transferring ownership of the live child. | After successful spawn, Tokio's piped-stderr and child-id Unix invariants are asserted with infallible extraction; `RunningSsh` immediately owns the child, process group, PTY, raw-stderr drain, FIFO reader, and private directory. | `ssh_admission_uses_private_fifo_and_never_raw_stderr` | `5547033` | Resolved at `5547033`; final re-review found only the hostname and failure-line findings tracked below. |
| Task 3 quality review | Minor | Derived Debug exposed prepared paths and snapshots; success matching accepted substrings without exact host/line binding. | Prepared maps/targets and FIFO/log structs use redacted custom Debug. Success requires a complete anchored line for the exact configured host and `using "publickey".`; prefix, suffix, other-host, cross-line, and partial forms remain pending. | `prepared_ssh_authority_is_complete_redacted_and_policy_denial_is_exact`; `ssh_authenticated_classifier_requires_exact_complete_expected_host_line` | `5547033` | Resolved at `5547033`; final re-review found only the hostname and failure-line findings tracked below. |
| Task 3 final quality re-review | Important | The classifier stored the raw configured host even though OpenSSH normalizes DNS names, and configuration admitted user-qualified/URI destination syntaxes. | Added a closed host-only grammar, preserved literal typed configuration, stored only canonical DNS/IP authority in prepared runtime state, derived classifier construction from `PreparedSshTarget`, and serialized canonical argv authority. RED: config accepted `user@host`; classifier compile RED lacked the prepared-owned constructor. GREEN covers uppercase DNS plus raw IPv4/IPv6 and rejects alternate destination syntaxes. | `ssh_host_grammar_preserves_literal_dns_and_rejects_alternate_destination_syntaxes`; `ssh_authenticated_classifier_requires_exact_complete_expected_host_line` | `c84cdb9` (`fix: canonicalize trusted SSH diagnostics (Refs #11)`) | Resolved at `c84cdb9`; spec and quality re-review confirmed the canonical authority fix. |
| Task 3 final quality re-review | Minor | Failure classes used transcript-wide substring searches, allowing embedded marker text to corrupt classification. | Match only complete supported OpenSSH failure lines; embedded, prefixed, suffixed, partial, and cross-line forms remain pending. | `ssh_failure_classifier_requires_complete_supported_lines` | `c84cdb9` | Resolved at `c84cdb9`; follow-up found only the omitted supported connection forms tracked below. |
| Task 3 spec and quality re-review | Important | The anchored failure grammar omitted common C-locale DNS resolution, pre-admission close/reset, and kex close forms, degrading them to generic failure. | Added complete authority-bound forms for `ssh: Could not resolve hostname ...`, `Connection closed by ...`, and `Connection reset by ...`, plus the exact fixed `kex_exchange_identification: Connection closed by remote host` line. Embedded, prefixed, suffixed, and cross-line variants remain pending. RED: the exact regression returned `None` for the DNS resolution line. GREEN: all supported forms return `ConnectionFailed` and all spoof-shaped variants remain `None`. | `ssh_failure_classifier_requires_complete_supported_lines` | `9d46049` (`fix: restore trusted SSH connection failures (Refs #11)`) | Resolved at `9d46049`; subsequent review found only the fixture findings tracked below. |
| Task 3 spec re-review | Minor | The FIFO admission fixture slept one second before a two-second deadline and timed out under the complete SSH module despite passing alone. | Replaced both sleeps with child-emitted barriers and explicit PTY tokens. After `READY`, the test synchronously polls the FIFO read once and proves it is pending before authorizing the first writer; the second phase advances only after the raw-stderr marker is observed. Every blocking operation has a phase-specific ten-second safety deadline, and the fixture closes both PTY endpoints before its bounded child reap. The exact test passed 10/10 independent invocations, the complete 30-test SSH module passed 3/3 independent invocations (90/90 aggregate), and the full crate passed 344/344. | `ssh_admission_uses_private_fifo_and_never_raw_stderr` | `8a3c91d` (`test: synchronize SSH FIFO admission fixture (Refs #11)`) | Resolved at `8a3c91d`; subsequent review found the EOF and teardown findings tracked below. |
| Task 3 spec re-review | Minor | The fixture's marker loops applied the deadline to each read and did not handle EOF, so an EOF before the marker could busy-loop forever. | Wrapped each complete marker search in one ten-second deadline and fail immediately when either source returns EOF before its marker. The exact test passed 10/10 independent invocations, the complete 30-test SSH module passed 3/3 independent invocations (90/90 aggregate), and the full crate passed 344/344. | `ssh_admission_uses_private_fifo_and_never_raw_stderr` | `4c6208d` (`test: bound SSH fixture marker searches (Refs #11)`) | Resolved at `4c6208d`; subsequent quality review found only the teardown race tracked below. |
| Task 3 quality re-review | Minor | The test dropped both PTY endpoints after receiving the final FIFO bytes but before the child had necessarily exited, so PTY closure could SIGHUP the fixture and make its successful-status assertion race. | The fixture now emits `DONE` only after writing the authenticated FIFO line. The test keeps both PTY endpoints alive, boundedly drains the terminal through `DONE` to EOF, asserts a successful bounded child wait, and only then drops the endpoints. The exact test passed 10/10 independent invocations, the complete 30-test SSH module passed 3/3 independent invocations (90/90 aggregate), and four consecutive full-crate runs passed 4/4 (1,376/1,376 aggregate), covering the reviewer's 1-in-4 reproduction window. | `ssh_admission_uses_private_fifo_and_never_raw_stderr` | `2cad2f9` (`test: retain PTY through SSH fixture exit (Refs #11)`) | Resolved at `2cad2f9`; immutable spec and quality re-reviews found zero Critical, Important, or Minor findings. |
| Task 4 self-review | Important | The first connecting `select!` placed a simultaneously ready authentication marker ahead of an already-requested shutdown/caller cancellation, which could create a false admitted lifecycle during teardown. | Kept authentication ahead of child exit so a genuine authenticated remote exit remains a normal post-admission result, but made shutdown/close and caller-drop branches higher priority than authentication. The 20-iteration cancellation and 20-iteration shutdown regressions remain green, while authenticated-then-exit still returns exit code 23 without a setup denial. | `cancelled_ssh_setup_is_registered_terminated_reaped_and_releases_capacity`; `shutdown_during_ssh_setup_terminates_reaps_and_releases_capacity`; `ssh_remote_exit_status_is_not_connection_setup_failure` | `feat: gate SSH lifecycle on authenticated admission (Refs #11)` | Resolved locally; the complete 50-test session module and escalated 355-test crate passed with zero failures. |
| Task 4 spec review | Important | Pre-spawn SSH construction failures mapped to `SpawnUnavailable`, but SessionManager audited only policy denial while WebSocket correctly suppressed manager-owned spawn denials; the combined path therefore emitted zero denials. | Added a prepared-material mutation seam that fails `SshSpawnSpec::build` before backend invocation. SessionManager now records exactly one `target/session-unavailable` denial for both direct and reserved pre-spawn construction failures; WebSocket remains a non-duplicating consumer. The regression proves zero spawn/lifecycle and immediate capacity release. | `ssh_pre_spawn_build_failure_has_one_denial_and_no_session_lifecycle` | `d3fafd1` (`fix: close SSH admission review gaps (Refs #11)`) | Exact review regression passed; session module passed 10 independent runs (510/510 aggregate). |
| Task 4 spec review | Minor | The scripted SSH fixture checked an atomic spawn count and then awaited `Notify`, allowing a notification in between and an unbounded lost-wakeup hang; spawned-task joins and adjacent phases also lacked complete deadlines. | Replaced the edge-triggered pair with replayable `watch` state. Spawn waits, task joins, shutdown, lifecycle receives, session close, and remote exit are wrapped in phase-labeled three-second safety deadlines; aborted joins must report cancellation. | `audit_failure_during_ssh_setup_terminates_and_reaps_without_authority`; complete `session::tests` module | `d3fafd1` (`fix: close SSH admission review gaps (Refs #11)`) | The formerly hanging exact audit-failure test passed, followed by 10 independent complete session-module runs (510/510 aggregate), with every run bounded by fixture phase deadlines. |
| Task 4 quality review | Important | The registered connecting task awaited admission supervision directly, so an unwind before `Supervisor` construction could drop child, drains, reservation, and persisted audit state without ordered cleanup or a matching end. | Keep a `ConnectingExecution` owner outside `catch_unwind`. Injected panics before admission and immediately after persisted start are recovered by cancellation, proven process-group cleanup, diagnostic joins, and result completion; only the persisted-start case records exactly one `supervisor-unwind` end, and neither case emits lifecycle events. | `connecting_unwind_before_handoff_terminates_reaps_and_releases_capacity`; `connecting_unwind_after_persisted_start_emits_one_end_and_reaps` | `be5ed24` (`fix: retain SSH cleanup ownership through failures (Refs #11)`) | Both exact regressions passed, followed by 10 independent complete session-module runs (580/580 aggregate). |
| Task 4 quality review | Important | Connecting and running cleanup paths discarded final process-group cleanup errors and then released capacity, allowing new ownership while cleanup was unproven. | Retry each bounded cleanup attempt under the still-registered supervisor task. The reservation remains owned during this explicit quarantine; caller cancellation and manager shutdown cannot discard it. A deterministic cleanup-proof seam covers connecting termination, admitted termination, both natural-exit group cleanup paths, and shutdown waiting until proof. | `cleanup_failure_quarantines_capacity_until_process_group_cleanup_is_proven`; `admitted_cleanup_failure_quarantines_capacity_until_group_cleanup_is_proven`; `natural_exit_cleanup_failure_keeps_connecting_capacity_quarantined`; `natural_exit_cleanup_failure_keeps_admitted_capacity_quarantined`; `shutdown_waits_for_quarantined_cleanup_owner_and_capacity_proof` | `be5ed24` (`fix: retain SSH cleanup ownership through failures (Refs #11)`) | Connecting quarantine passed 20/20 in-process, admitted quarantine passed 10/10, and both natural-exit plus shutdown exact regressions passed before final verification. |
| Task 4 quality review | Minor | Task 4 evidence still used pending placeholders after the immutable `d3fafd1` review-fix commit. | Replaced both stale placeholders with `d3fafd1` and added explicit rows for the quality-review RED/GREEN evidence and final remediation SHA bookkeeping. | N/A (evidence-only correction; no behavior regression required) | `be5ed24` (`fix: retain SSH cleanup ownership through failures (Refs #11)`) | Resolved by immutable remediation SHA bookkeeping. |
| Task 4 spec re-review | Important | An observed post-audit unwind run timed out in cleanup, and inspection proved `PtyChild::terminate_with` bounded only the pre-SIGKILL graceful wait while its mandatory post-SIGKILL `Child::wait` could await forever. That contradicted the bounded-attempt evidence even though detached reproduction later passed the post-audit regression. | Added a deterministic post-SIGKILL wait-stall seam. The final reap now has the same bounded grace deadline as the graceful phase, returns a retryable backend error on expiry, and caches the first successfully reaped exit status so later cleanup attempts never wait on an already-reaped child. Quarantine ownership and capacity retention remain unchanged until a complete signal/reap attempt succeeds. | `post_sigkill_child_wait_is_bounded_and_retryable`; `connecting_unwind_after_persisted_start_emits_one_end_and_reaps` | `1dd164d` (`fix: bound SSH child reap retries (Refs #11)`) | Backend RED failed 0/1 after exceeding the 300 ms outer deadline. GREEN proves a bounded first error and a successful retry while the injected post-kill wait remains blocked; post-audit unwind passed 50 independent processes and the complete session module passed 10 independent processes (580/580). |
| Task 4 final quality re-review | Important | Teardown treated a successful group signal plus direct-child wait as cleanup proof. It did not require real process-group absence, and retrying a nonzero signal after leader reap could target a reused numeric PGID. | Added a monotonic teardown state machine. The unreaped leader pins the identifier for exactly one final SIGKILL; child status is cached; every post-reap attempt is signal-zero only; ESRCH alone proves absence, while success and EPERM remain quarantined. The real resistant-group regression requires leader, descendant, and PGID absence before capacity release and second admission. | `reaped_group_cleanup_only_probes_and_never_resignals_reused_pgid`; `capacity_is_held_until_resistant_group_teardown_finishes` | `ad1c8f9` (`fix: prove SSH process-group cleanup (Refs #11)`) | Deterministic ops regression asserts one SIGKILL, four probes with three present responses, and zero post-reap nonzero signals. The real regression passed 20 independent processes. |
| Task 4 final quality re-review | Important | `run_connecting` removed the connecting context and process parts from the unwind-owned execution state before complete supervisor construction, so a panic in the handoff interval could drop child and capacity outside recovery. | Added an explicit `ConnectingHandoff` retained inside `ConnectingExecution` through lifecycle and worker construction. Recovery owns child, reservation, audit, diagnostic drains, any already-started worker handles, and matching lifecycle closure if construction had emitted lifecycle start events. Only an infallible move-only assembly remains after handoff removal. | `connecting_unwind_after_handoff_take_retains_cleanup_and_audit_owner` | `ad1c8f9` (`fix: prove SSH process-group cleanup (Refs #11)`) | RED failed 0/1 because child reap timed out. GREEN proves reap/group absence, capacity and registry release, no unmatched lifecycle, and exactly one correlated unwind audit end. Phase-specific deadlines distinguish cleanup proof from subsequent result delivery, and the ownership test uses a single-process child so descendant disappearance remains isolated to the dedicated real process-group regression. The session module passed 10 independent processes (590/590), then three complete workspace runs passed 1,098/1,098 aggregate. |
| Task 4 final spec re-review | Important | Natural-exit selection called `Child::wait` before group cleanup, reaping the leader and losing the safe PID/PGID pin. The subsequent probe-only path could never terminate a HUP-resistant descendant, while making the fixture kill its own descendant masked the established local-PTY cleanup contract. | Restored the original fixture contract. A safe nonblocking Rustix `waitid(P_PID, WEXITED | WNOHANG | WNOWAIT)` observer drives `Running → ExitObservedUnreaped`; natural and requested teardown send HUP, wait the bounded grace period, send one final SIGKILL while the leader remains unreaped, then reap/cache and use only signal-zero ESRCH probes. | `natural_exit_is_observed_then_group_signaled_before_leader_reap`; `requested_teardown_observes_graceful_exit_before_final_signal_and_reap`; `natural_leader_exit_cleans_resistant_group_before_capacity_release`; `normal_exit_has_exactly_one_audit_completion` | `3a099fc` (`fix: clean natural SSH process groups (Refs #11)`) | Both real tests failed 0/1 at the closed-event deadline with original fixture semantics. Deterministic GREEN proves exact observe/HUP/SIGKILL/reap/probe ordering and no pre-signal reap. The real code-23 leader and same-PGID resistant descendant record HUP while capacity is still held, then require leader, descendant, and PGID ESRCH before second admission; it passed 20 independent processes. Existing WebSocket code-0 fixture behavior remains unchanged. Three complete workspace runs passed 1,107/1,107 aggregate, and cargo-deny accepted the existing Rustix dependency's added process feature. |
| Task 4 final immutable spec and quality reviews | None | Both independent reviews inspected immutable evidence head `c7a889a` and reported zero Critical, Important, or Minor findings. | No remediation required. Task 4 is review-complete. | N/A | `c7a889a` (`docs: record natural-exit cleanup evidence (Refs #11)`) | The spec reviewer freshly passed backend 9/9, the exact natural-resistant regression 1/1, and session 59/59; diff/status were clean and no fixture survivors remained. The quality reviewer freshly passed backend 9/9, session 59/59, PTY integration 11/11, the exact resistant regression, Clippy with warnings denied, and diff check; no fixture survivors remained. Both final verdicts were clean. |
| Task 5 quality review | Important | The original admission test called the support predicate directly, so reverting the real WebSocket branch to PTY-only would not fail it; the integration bridge suite also had no prepared SSH target reaching SessionManager. | Replaced the direct predicate assertion with a loopback WebSocket regression using a ticket-bound prepared SSH target and controlled fake SSH executable. The client sends only the unchanged ticket envelope. A gate mutation returns `session-denied`; production reaches SessionManager, classifies the private client-log FIFO, and returns the curated host-key code. The test also proves one manager-owned denial, no lifecycle, and no host/user/path/argv/diagnostic reflection. | `websocket_admits_configured_ssh_targets_without_new_authority_fields` | `bca52b0` (`test: prove WebSocket reaches prepared SSH (Refs #11)`) | Resolved at immutable review head `0c5c2c9`; both Task 5 re-reviews found zero Critical, Important, or Minor findings. |
| Task 5 quality review | Minor | The Task 5 evidence named only a commit subject instead of immutable implementation and remediation SHAs. | Recorded immutable `f1f8dc2` and `bca52b0` SHAs in the Task 5 evidence and review rows. | N/A (evidence correction) | `bca52b0` (`test: prove WebSocket reaches prepared SSH (Refs #11)`) | Resolved at immutable review head `0c5c2c9`; both Task 5 re-reviews confirmed the evidence SHAs are accurate. |
| Task 5 immutable spec re-review | None | Review of immutable head `0c5c2c9` found zero Critical, Important, or Minor findings. | No remediation required. Task 5 is spec-review complete. | N/A | `0c5c2c9` (`docs: record Task 5 review remediation (Refs #11)`) | The exact prepared-SSH WebSocket regression passed 1/1 and the WebSocket module passed 16/16. Protocol and frontend sources were unchanged by remediation; diff and status were clean; evidence SHAs were accurate; and no fake SSH or sleep process survived. |
| Task 5 immutable quality/security re-review | None | Review of immutable head `0c5c2c9` found zero Critical, Important, or Minor findings. | No remediation required. Task 5 is quality/security-review complete. | N/A | `0c5c2c9` (`docs: record Task 5 review remediation (Refs #11)`) | The exact prepared-SSH WebSocket regression passed 1/1, the WebSocket module passed 16/16, Clippy for all ttygated targets passed with warnings denied, and formatting, diff, and status checks were clean. |
| Task 6 real compatibility RED | Important | Installed OpenSSH 10.2 emits the changed-host marker in the private `-E` log as the complete decorated line `@    WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED!     @`; the classifier recognized only the undecorated form, so a real mismatch fell back to generic failure. | Added only the exact installed complete-line variant. Prefixes, suffixes, partial, embedded, cross-line, and raw-stderr text remain rejected; the real mismatch now retains the stable safe frontend category and distinct audit reason. | `mismatched_host_key_has_stable_frontend_and_audit_failure`; installed decorated-line unit assertion in `ssh_classifier_exposes_live_setup_classification_before_eof` | `f94dd9d` | The real mismatch now passes through the production HTTP-ticket/WebSocket path with exact safe code/message, distinct audit reason, and 20/20 independent-process evidence. |
| Task 6 fixture self-review | Important | Docker port publication is not listener readiness. Repetition 20 of the first cancellation campaign connected before sshd accepted and failed with `SshConnectionFailed`. | Fixture readiness now requires both a published random loopback mapping and a successful TCP connection before returning the fixture. | `ssh_idle_absolute_explicit_cancellation_shutdown_and_unwind_complete_once` | `f94dd9d` | A fresh 20-independent-process cancellation/lifecycle campaign passed 20/20; each run removed its unique container. |
| Task 6 fixture self-review | Important | Cleanup originally became armed only after the async readiness wait returned, so cancellation or panic during readiness could strand an already-started container. | A dedicated container guard is constructed immediately after `docker run`; remediation orders container guard, temporary directory, then async serial lock, and a cancellation regression proves all three release in that order. | `cancelled_fixture_construction_removes_container_materials_and_releases_lock` | `f94dd9d`; strengthened at `9e879a8` | The exact cancellation regression and complete 15-test real suite passed; final Docker/material/process inspection was empty. |
| Task 6 spec review | Important | The combined lifecycle test did not exercise unwind and only compared aggregate start/end counts; caller cancellation had no exact denial evidence. | Real scenarios now assert a unique session ID, exactly one correlated start/end, and the expected close reason independently. Pre-admission caller cancellation records exactly one `target/session-cancelled` denial and zero lifecycle. Actual unwind remains behind private unit seams: three connecting panic points and admitted supervisor unwind all pass without widening production authority. | `ssh_idle_absolute_explicit_cancellation_shutdown_and_unwind_complete_once`; `cancelled_ssh_setup_is_registered_terminated_reaped_and_releases_capacity`; `cleanup_failure_quarantines_capacity_until_process_group_cleanup_is_proven`; `connecting_unwind_*`; `supervisor_unwind_reaps_child_and_has_exactly_one_audit_completion` | `9e879a8` | Real lifecycle/cancellation passed 20/20; connecting unwind passed 3/3 and admitted supervisor unwind passed 1/1. |
| Task 6 spec review | Important | Real mismatch/authentication and secrecy stopped at `SessionError`/`Display`, so they did not prove production frontend controls or complete audit leakage boundaries. | Both denials now traverse HTTP ticket issuance and WebSocket redemption, decode exact curated controls, and assert one denial with zero lifecycle. Sentinel-bearing known-host content/path/public data, identity path/private envelope comment, audit path, cookie, ticket, header, banner, host, user, and complete private-key lines are absent from both rendered controls and complete audit JSONL. | `mismatched_host_key_has_stable_frontend_and_audit_failure`; `wrong_identity_key_is_distinct_authentication_failure`; `ssh_secret_sentinels_never_reach_audit_or_frontend_errors` | `9e879a8` | All exact regressions and the complete 15-test real suite passed; mismatch passed 20/20 independent processes. |
| Task 6 final spec review | Important | The secrecy test listed a fixed argv string that was never passed to OpenSSH and set an environment marker only inside sshd, so its argv/environment non-reflection claims were vacuous. | A unit regression renames the actual prepared known-host and identity files with sentinel-bearing paths, constructs the production `SshSpawnSpec`, and proves both exact dynamic option values are present in runtime argv. The integration regression self-spawns the current test binary with an ambient environment sentinel, prevents recursion with a private child flag, traverses real HTTP/WebSocket/SSH/audit behavior in the child, proves the variable is absent from the remote environment, and scans complete audit/frontend output for the ambient value, actual dynamic argv, and fixed client environment/options. This avoids unsafe concurrent process-environment mutation. | `secrecy_sentinel_is_an_actual_dynamic_runtime_argv_value`; `ssh_secret_sentinels_never_reach_audit_or_frontend_errors` | `032c786` | Both regressions passed exactly; the complete real suite passed 15/15, the full crate passed 388/388, Clippy/format/diff/cargo-deny passed, and no Docker container, SSH process, or temporary fixture artifact survived. |
| Task 6 final quality review | Important | The malicious banner used text that the admission classifier would not accept, leaving its anti-forgery claim vacuous. | The real banner is now the exact accepted line `Authenticated to 127.0.0.1 using "publickey".`; a wrong identity still produces exactly one authentication denial and zero lifecycle. A private unit oracle separately proves those exact raw-stderr bytes classify as authenticated while the actual FIFO-fed classifier remains pending and then observes authentication denial. No production seam was added. | `malicious_pre_auth_banner_cannot_forge_ssh_admission`; `ssh_admission_uses_private_fifo_and_never_raw_stderr` | `ff305da` | Both focused regressions passed; the real banner regression passed 20/20 fresh processes and the complete real suite passed 17/17. |
| Task 6 final quality review | Important | One host directory containing client private keys, wrong-host material, known-host files, and audit output was mounted into sshd, and temporary roots appeared at the worktree top level. | Material is split into private client-only and server-only 0700 directories under ignored `target/ssh-fixtures`. The server directory contains exactly one 0600 host private key, the accepted client public key, and banner; only that directory mounts read-only. Local and Docker-backed allowlist assertions prove exact names and exclude client/audit/known-host material. Cancellation proves relay, sshd, network, material, and lock cleanup. | `sshd_fixture_mount_and_runtime_are_minimal_and_isolated`; `cancelled_fixture_construction_removes_container_materials_and_releases_lock` | `ff305da` | Both exact regressions and the complete 17-test real suite passed; final material and Docker scans were empty. |
| Task 6 final quality review | Important | The secrecy loop interpolated secret values into assertion diagnostics, and the self-spawn parent replayed child output on failure. | All scans use a `(label, value)` helper whose failure text contains only the surface and label. A deliberate caught failure proves the label is present and value absent. The self-spawn parent reports only child status and never relays captured stdout/stderr. | `secret_scan_failure_reports_label_not_value`; `ssh_secret_sentinels_never_reach_audit_or_frontend_errors` | `ff305da` | Both focused regressions, the complete real suite, and the full 390-test crate passed; final sentinel scans found no fixture/output residue. |
| Task 6 final quality review | Minor | The Docker fixture used a mutable base tag, unversioned packages, a mutable local image tag, default networking/rootfs/capabilities, and had no runtime assertions for those controls. | Pinned Alpine's multi-architecture digest plus exact OpenSSH, procps-ng, and socat versions; validated installed content; captured and validated the sha256 build ID and ran it directly. sshd is internal-only with no outbound route, read-only rootfs, minimal tmpfs, no-new-privileges, PID limit, all capabilities dropped plus only CHOWN/SETGID/SETUID/SYS_CHROOT. Because Colima suppresses published ports on internal networks, a separate mount-free/capability-free/read-only relay is bridge+internal and publishes loopback only. Runtime inspection asserts both immutable image IDs, networks, mounts, routes, rootfs, capabilities, security options, PID limits, and binding. | `sshd_fixture_mount_and_runtime_are_minimal_and_isolated`; `strict_known_host_ssh_reaches_real_sshd` | `ff305da` | The runtime inspection and strict-success tests passed exactly; 17/17 real and 390/390 full-crate verification passed without survivors. |
| Task 6 final immutable spec re-review | None | Review of immutable evidence head `6991a34` found zero Critical, Important, or Minor findings. | No remediation required. Task 6 is spec-review complete; all prior Task 6 requirements remain closed. | N/A | `6991a34` (`docs: record hardened SSH fixture evidence (Refs #11)`) | Fresh evidence passed the hardened real suite 17/17 and exact FIFO/raw-stderr oracle 1/1. No containers, networks, SSH/sshd/socat processes, or fixture artifacts survived; worktree status and diff checks were clean. |
| Task 6 final immutable quality review | None | Review of immutable evidence head `6991a34` found zero Critical, Important, or Minor findings. | No remediation required. Task 6 is quality-review complete. | N/A | `6991a34` (`docs: record hardened SSH fixture evidence (Refs #11)`) | Fresh evidence passed the hardened real suite 17/17 plus focused FIFO oracle 1/1, secret-diagnostic 1/1, runtime/mount 1/1, banner 1/1, and construction-cancellation 1/1. No fixture containers, networks, SSH/sshd/test processes, material, or worktree changes survived; diff check was clean. |
| Task 6 spec review | Minor | Task 6 evidence cited subjects rather than immutable SHAs. | Replaced subject-only evidence with immutable implementation `f94dd9d`, remediation `9e879a8`, and evidence `4eb9938` references. | N/A (evidence-only) | `4eb9938` | Immutable SHA bookkeeping is complete. |
| Task 7 security review | Critical | OpenSSH re-lexes `-o` option values with its own quoting, whitespace, percent-token, and tilde expansion rules, so configured `identity_file`/`known_hosts` paths containing those constructs would be re-tokenized or expanded instead of used literally, silently changing which private key or host database the strict policy trusts. | Added `validate_ssh_option_path`, layered on `validate_literal_path`, at both parse-time conversion and typed-target validation for exactly `identity_file` and `known_hosts`; it rejects empty values, non-UTF-8, all whitespace and control characters, `%`, single and double quotes, backslash, `$`, and a leading `~`. `ssh_executable` intentionally remains under the direct-executable policy because it is executed directly and never re-lexed by OpenSSH; a space-bearing executable path is still accepted and covered by regression. The pre-existing module case that accepted a space-bearing known-hosts path was corrected to the fail-closed grammar. RED: `ssh_option_paths_reject_every_lexer_or_expansion_construct` failed because `/etc/ttygate/id key` was accepted. GREEN: the exact regression, the complete config module (44/44), and `installed_openssh_renders_safe_material_option_paths_exactly` — which proves installed OpenSSH `-G` renders every accepted material path byte-identically — all passed. | `ssh_option_paths_reject_every_lexer_or_expansion_construct`; `installed_openssh_renders_safe_material_option_paths_exactly` | `fix: address OpenSSH security review findings (Refs #11)` | Pending fresh independent full-branch re-review. |
| Task 7 security review | Important | OpenSSH implicitly loads a sibling `<identity>-cert.pub` next to the configured identity, so an attacker or operator mistake that places a signed certificate beside the pinned key silently changes the authentication method without any configuration change. | Pinned `CertificateFile=/dev/null` as the 29th entry of the strict option source of truth. The capability probe argv, canonical output validation (`certificatefile /dev/null`), production argv literal pass-through, exact-once serialization with literal pinned-value assertion, and the installed-OpenSSH rendering test all cover the pin. The hardened real-sshd fixture now generates a user CA, mounts only `user_ca.pub` into the server-only fixture directory (mount allowlist and runtime mount assertion updated accordingly), and configures `TrustedUserCAKeys`. RED: the capability vocabulary test expected 58 option argv entries but observed 56 because `CertificateFile` was absent, and the negative certificate-policy test authenticated through the conventional sibling certificate before the pin (no remote command was executed). GREEN: the vocabulary test passed 1/1 and `ca_only_sibling_identity_certificate_is_rejected_without_session_lifecycle` proves against real sshd that a conventionally named signed sibling certificate now yields exactly one authentication denial and zero session lifecycle. | `capability_probe_covers_and_validates_complete_strict_runtime_vocabulary`; `ssh_argv_serializes_every_pinned_option_exactly_once`; `installed_openssh_renders_safe_material_option_paths_exactly`; `ca_only_sibling_identity_certificate_is_rejected_without_session_lifecycle`; `sshd_fixture_mount_and_runtime_are_minimal_and_isolated` | `fix: address OpenSSH security review findings (Refs #11)` | Pending fresh independent full-branch re-review. |
| Task 7 security review | Minor | Public documentation still describes SSH support as unimplemented and omits the strict policy, provisioning, compatibility, and residual-risk truth. | Deferred by design to Task 8, which adds failing public-doc assertions before updating `README.md`, `docs/roadmap.md`, `docs/ttygate-rewrite-plan.md`, `docs/threat-model.md`, `docs/protocol.md`, and `scripts/check-public-docs.sh`. | N/A (Task 8 public-doc assertions) | `docs: document strict OpenSSH targets (Refs #11)` | Pending Task 8 completion. |
| Task 7 security review | Minor | Plan evidence rows embedded machine-specific `/Users/...` toolchain paths. | Replaced the absolute toolchain `PATH` command text with machine-neutral "explicit stable-toolchain `PATH`" wording; no machine-specific absolute paths remain anywhere in the plan. | N/A (evidence-only correction) | `fix: address OpenSSH security review findings (Refs #11)` | Pending fresh independent full-branch re-review. |

## Publication evidence

| Check | Result |
|---|---|
| Local verified HEAD | Pending |
| Remote branch HEAD | Pending |
| Draft PR URL/state/base/head/body/SHA | Pending |
| Pending GitHub checks | Pending |
